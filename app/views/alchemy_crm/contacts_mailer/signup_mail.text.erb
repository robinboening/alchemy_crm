<%= @element.content_by_name('text').essence.stripped_body % {
	:link => alchemy_crm.verify_contact_url(:token => @contact.email_sha1, :element_id => @element.id, :newsletter_ids => @newsletter_ids),
	:name => @contact.fullname
} %>
